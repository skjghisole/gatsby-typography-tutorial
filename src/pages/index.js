import React from "react"
import Layout from "../components/Layout"

export default () => (
	<Layout>
		<h1>(Tutorial part three) Typography Example</h1>
		<p>
			This the third part of the tutorial, we implemented Typography to our
			gatsby-config and made our very own custom Typography utility which is
			stored in src/utils/typography.
		</p>
	</Layout>
)
